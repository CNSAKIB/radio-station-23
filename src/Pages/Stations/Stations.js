import React, { useState, useEffect } from 'react';
import { Button } from 'react-bootstrap';
const Stations = () => {

    return (
        <div>
            <h1>This is Stations</h1>
            <Button variant="primary">Primary</Button>
        </div>
    );
};

export default Stations;